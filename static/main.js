/* DM Post-Processor — frontend logic (stub, full implementation in Phase 3) */

/* ── Startup ── */
// GET /api/version  → set version badge
// GET /api/system_info → set worker slider max
// GET /api/config   → populate all fields, apply theme

document.addEventListener('DOMContentLoaded', () => {
  // Phase 3: initialise app
});
